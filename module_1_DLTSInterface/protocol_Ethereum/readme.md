# Ethereum DLTs interface module components
The presented tools allow to parse the raw blockchain data synched from bitcoin core into the defined custom format.

## ethereum-parser

To parse the Ethereum data and encode it into a Neo4j dedidacted format.